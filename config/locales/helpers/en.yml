---
en:
  helpers:
    currency:
      euro: "€"
      usd: "$"
    flash:
      create:
        error: "%{model} not created."
        success: "%{model} created successfully."
      identities:
        destroy:
          error: Service could not be disconnected.
          success: Service disconnected successfully.
      subscriptions:
        new:
          error: Something went wrong. Please try again.
      update:
        error: "%{model} not updated."
        success: "%{model} updated successfully."
      users/omniauth_callbacks:
        google_oauth2:
          error: Could not authenticate you from Google account.
          success: Successfully authenticated from Google account.
    forms:
      submit:
        required: All fields marked with * are required.
    meta:
      description:
        default: Rails boilerplate to faster bootstrap new projects.
      keywords:
        default: Ruby on Rails, Boilerplate
      robots:
        default: all
      title:
        default: Rails Boilerplate
    pundit:
      not_authenticated: You need to sign in or sign up before continuing.
      not_authorized: You are not authorized to perform this action.
    sso:
      disconnect: Disconnect %{provider}
      google_oauth2: Google
      sign_in_with: Sign in with %{provider}
    time:
      day: day
      month: month
      quarter: quarter
      year: year
