development:
  adapter: redis
  url: <%= Settings.redis_url %>

test:
  adapter: test

production:
  adapter: redis
  url: <%= Settings.redis_url %>
  channel_prefix: <%= "#{Settings.app_name}_production" %>
