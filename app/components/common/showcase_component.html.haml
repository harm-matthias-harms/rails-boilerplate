.hero.my-10
  .hero-content
    = render_component(Common::CardComponent, classes: '!w-full') do
      .px-10.py-10.grid.grid-cols-1.lg:grid-cols-2.gap-10.lg:gap-20
        .order-last.lg:order-first
          = heading
          = description
          = list
        = image
