= render_component(Mailer::TextComponent) do
  We wanted to notify you that your password for #{Settings.app.name.humanize} has been changed.
= render_component(Mailer::TextComponent) do
  If this change was authorized by you, no further action is needed.
= render_component(Mailer::TextComponent) do
  If you did not request this change, please contact our support team at #{Settings.mail.support}
  immediately to secure your account.
