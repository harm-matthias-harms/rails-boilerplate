= render_component(Mailer::TextComponent) { t('.instructions.1', app: Settings.app.name.humanize) }
= render_component(Mailer::TextComponent) { t('.instructions.2') }
= render_component(Mailer::ButtonComponent, href: confirmation_url(@resource, confirmation_token: @token)) do
  = t('.confirm')
= render_component(Mailer::TextComponent) { t('.instructions.3', app: Settings.app.name.humanize) }
